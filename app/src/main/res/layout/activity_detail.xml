<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="@android:dimen/app_icon_size"
    tools:context=".Detail">


    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/detail_foto"
            android:layout_width="375dp"
            android:layout_height="210dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@tools:sample/avatars"/>

        <TextView
            android:id="@+id/detail_nama_barang"
            android:layout_width="275dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:gravity="center"
            android:textStyle="bold"
            android:textSize="24sp"
            app:layout_constraintStart_toStartOf="@id/detail_foto"
            app:layout_constraintEnd_toEndOf="@id/detail_foto"
            app:layout_constraintTop_toBottomOf="@id/detail_foto"
            tools:text="@string/nama_barang"/>

        <TextView
            android:id="@+id/detail_desc_barang"
            android:layout_width="375dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="20dp"
            android:justificationMode="inter_word"
            android:textSize="18sp"
            app:layout_constraintStart_toStartOf="@id/detail_nama_barang"
            app:layout_constraintEnd_toEndOf="@id/detail_nama_barang"
            app:layout_constraintTop_toBottomOf="@id/detail_nama_barang"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="@string/deskripsi"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>

